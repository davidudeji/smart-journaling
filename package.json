{
  "name": "smart-peak-journal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"const fs=require('fs'); const path=require('path'); const out='dist'; fs.mkdirSync(out,{recursive:true}); ['index.html','styles.css','app-logic.js'].forEach((file)=>fs.copyFileSync(file, path.join(out, file)));\""
  }
}
